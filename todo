Add config to Window class
	Borderless
	Dropfile enabled

Add classes to deal with JSON/XML/INI + common interface for adding new formats
	Lookup binary formats

Data serialization
	To JSON/XML/...

Filesystem
	Directory traversing
	File IO

	JSON/XML/INI loading/writing - implicitly available with parsing classes 

Renderable components/interface
	Instances go into render queues, referenced in renderer components

Ensure that entities can ONLY be destroyed through the scene using DestroyEntity